{
  "schema_version": 1,
  "suite_id": "single_instruction.data_residency_128b",
  "suite_version": "v5",
  "measurement_release": "ColdThenHotV1",
  "supersedes": "v4; v4 showed that prefetch.tensormap alone does not produce the same state as a completed TMA, so v5 uses an untimed same-map TMA primer",
  "scope": "single_instruction",
  "project": "data_residency_128b",
  "variants": ["bulk-tensor__g2s-s2g__128b-residency-controls"],
  "formal": {"warmups": 0, "repeats": 2, "paid_attempts": 1},
  "expected_cases": {"h100": 12, "b200": 12},
  "wait_method": "S2G wait_group 0; G2S direct mbarrier.test_wait polling",
  "pairing": "one new TensorMap per Tensor case; two commands execute in the same kernel under cold-hot, cold-cold or hot-hot payload preparation",
  "data_residency": "cold scans >=2x L2 before the command; hot loads the exact target line with ld.global.cg; cold-hot does no preparation between commands so repeat 1 immediately reuses repeat 0's line",
  "descriptor_residency": "an untimed TMA using the exact same TensorMap completes before payload preparation; measured commands therefore begin after one prior TMA use",
  "direction_definition": "bulk/tensor crossed with g2s/s2g",
  "context_definition": "one issuing thread, one measured command at a time",
  "sources": ["cuda_data_residency_128b.cu", "modal_data_residency_128b.py"],
  "commands": {
    "h100": ["{modal}", "run", "{suite}/modal_data_residency_128b.py", "--gpu", "h100", "--output", "{output}"],
    "b200": ["{modal}", "run", "{suite}/modal_data_residency_128b.py", "--gpu", "b200", "--output", "{output}"]
  }
}
