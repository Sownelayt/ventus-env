{
  "schema_version": 1,
  "suite_id": "single_instruction.tensormap_decode",
  "suite_version": "v4",
  "measurement_release": "ColdThenHotV1",
  "supersedes": "v3; v3 used two independent descriptors for repeat 0/1",
  "scope": "single_instruction",
  "project": "tensormap_decode",
  "variants": ["bulk-tensor__g2s-s2g__rank1-5"],
  "formal": {"warmups": 0, "repeats": 2, "paid_attempts": 1},
  "expected_cases": {"h100": 40, "b200": 40, "ventus": 20},
  "pairing": "one new TensorMap per case; two same-kernel consecutive commands use it as first-use then reuse",
  "wait_method": "S2G wait_group 0 primary; G2S direct mbarrier.test_wait auxiliary",
  "data_residency": "128B payload control matched against Bulk",
  "descriptor_residency": "natural cases are L2-hot but TMAU-cold before repeat 0; repeat 1 immediately reuses the same map; explicit prefetch is separately labeled",
  "direction_definition": "g2s and s2g reported separately",
  "context_definition": "one issuing thread, one measured command at a time",
  "sources": [
    "cuda_tensormap_decode.cu",
    "audit_instruction_stream.py",
    "modal_tensormap_decode.py",
    "ventus_tensormap_decode.cpp",
    "ventus_tensormap_decode.cl",
    "run_ventus_decode_short.py"
  ],
  "commands": {
    "h100": ["{modal}", "run", "{suite}/modal_tensormap_decode.py", "--gpu", "h100", "--output", "{output}"],
    "b200": ["{modal}", "run", "{suite}/modal_tensormap_decode.py", "--gpu", "b200", "--output", "{output}"],
    "ventus": ["python3", "{suite}/run_ventus_decode_short.py", "--output", "{output}"]
  }
}
