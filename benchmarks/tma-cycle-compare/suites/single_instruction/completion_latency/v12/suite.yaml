{
  "schema_version": 1,
  "suite_id": "single_instruction.completion_latency",
  "suite_version": "v12",
  "measurement_release": "ColdThenHotV1",
  "supersedes": "v11; V12 replaces the 23-29-cycle grid with one fixed 640-position scan targeting a three-cycle step",
  "scope": "single_instruction",
  "project": "completion_latency",
  "variants": [
    "bulk-tensor__g2s__fresh-map-common",
    "tensor__g2s__fresh-map-2d"
  ],
  "formal": {"warmups": 0, "repeats": 2, "paid_attempts": 1},
  "pairing": "one new TensorMap per delay point; one kernel first-uses it, completes cleanup, then immediately reuses the exact same address and contents",
  "expected_cases": {
    "h100": {
      "bulk-tensor__g2s__fresh-map-common": 155520,
      "tensor__g2s__fresh-map-2d": 116480
    },
    "b200": {
      "bulk-tensor__g2s__fresh-map-common": 155520,
      "tensor__g2s__fresh-map-2d": 116480
    }
  },
  "wait_method": "one mbarrier.test_wait per command in a same-map cold/hot pair; one fixed non-adaptive 640-position scan encoded as 80 static 8-position kernel banks selected outside timing; no indexed jump in the timed path, whose requested step adds one predicate, one uniform branch and one retained PM-event; report separate midpoint values and measured clock64 spacing",
  "data_residency": "payload L2-hot through ordinary ld.global.cg before the cold/hot pair",
  "descriptor_residency": "L2-hot through ordinary ld.global.cg; no prior TMA use before repeat 0; repeat 1 reuses the exact same descriptor; explicit TensorMap prefetch only in the separate control mode",
  "direction_definition": "G2S only; issue begins at the cp.async.bulk instruction and completion is observed with one nonblocking test_wait",
  "context_definition": "one CTA and one issuing thread per pair; eight predetermined case shards bound hang cost without splitting or adapting any 640-point scan; each point has a new global base and 4 KiB-spaced TensorMap within its context, and the barrier is reinitialized before the second command",
  "sources": [
    "cuda_g2s_fresh_map.cu",
    "modal_completion_latency.py",
    "audit_instruction_stream.py",
    "generate_delay_ladder.py",
    "delay_ladder_v12.inc",
    "METHOD.md",
    "../../common_matrix/v3/tma_comprehensive_matrix.h"
  ],
  "commands": {
    "h100": ["{modal}", "run", "{suite}/modal_completion_latency.py", "--gpu", "h100", "--variant", "{variant}", "--output", "{output}"],
    "b200": ["{modal}", "run", "{suite}/modal_completion_latency.py", "--gpu", "b200", "--variant", "{variant}", "--output", "{output}"]
  }
}
