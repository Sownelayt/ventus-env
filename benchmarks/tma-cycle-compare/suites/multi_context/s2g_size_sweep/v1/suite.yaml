{
  "schema_version": 1,
  "suite_id": "multi_context.s2g_size_sweep",
  "suite_version": "v1",
  "measurement_release": "ColdThenHotV1",
  "supersedes": "new isolated size sweep derived from multi_context.same_direction v4",
  "scope": "multi_context",
  "project": "s2g_size_sweep",
  "variants": ["s2g"],
  "formal": {"warmups": 0, "repeats": 2, "paid_attempts": 1},
  "pairing": "one new same-map TensorMap per byte-length/command-count case; repeat 0 first-uses it and repeat 1 immediately reuses the exact same descriptor in one kernel",
  "expected_cases": {"h100": 60, "b200": 60},
  "wait_method": "issue all S2G commands, commit once, then cp.async.bulk.wait_group 0; total batch completion cycles",
  "data_residency": "each command owns a disjoint shared region and destination rows; only payload bytes vary",
  "descriptor_residency": "each case owns a new same-map TensorMap; ordinary loads may make descriptor bytes L2-hot before repeat 0, while repeat 1 immediately reuses the same address and contents",
  "direction_definition": "S2G only",
  "context_definition": "one issuing thread batches N=1/2/4/8/16/32 commands in one CTA",
  "coverage": "Tensor, same-map, batched, intra-CTA; 128/256/512/1024/1536/2048/2560/3072/3584/4096 bytes",
  "sources": [
    "cuda_multi_context.cu",
    "modal_multi_context.py"
  ],
  "commands": {
    "h100": ["{modal}", "run", "{suite}/modal_multi_context.py", "--gpu", "h100", "--project", "same", "--direction", "s2g", "--output", "{output}"],
    "b200": ["{modal}", "run", "{suite}/modal_multi_context.py", "--gpu", "b200", "--project", "same", "--direction", "s2g", "--output", "{output}"]
  }
}
