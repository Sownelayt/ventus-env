{
  "schema_version": 1,
  "suite_id": "multi_context.mixed_direction",
  "suite_version": "v3",
  "measurement_release": "ColdThenHotV1",
  "supersedes": "all v1/v2 data; v2 incorrectly gave repeat 0 and repeat 1 different cold TensorMap banks",
  "scope": "multi_context",
  "project": "mixed_direction",
  "variants": ["mixed"],
  "formal": {"warmups": 0, "repeats": 2, "paid_attempts": 1},
  "pairing": "one new load/store TensorMap bank pair per case; repeat 0 first-uses it and repeat 1 immediately reuses the exact same banks in one kernel",
  "expected_cases": {"h100": 270, "b200": 270, "ventus": 124},
  "resource_limited_cases": {"ventus": 146},
  "wait_method": "per-command independent completion objects; batch completion span",
  "data_residency": "distinct global lines and shared regions; no line-merge aliasing",
  "descriptor_residency": "each case owns new load/store banks; ordinary loads may make descriptor bytes L2-hot, but no TMA/prefetch touches them before repeat 0; repeat 1 reuses the same addresses and contents",
  "direction_definition": "alternating, all g2s then s2g, and all s2g then g2s",
  "context_definition": "same issuing thread serial/batched or occupancy-safe independent CTAs, pairs=1/2/4/8/16",
  "coverage": "alternating, G2S-then-S2G and S2G-then-G2S at intra-CTA and cooperative multi-CTA levels",
  "sources": [
    "cuda_multi_context.cu",
    "modal_multi_context.py",
    "ventus_multi_context.cpp",
    "ventus_multi_context.cl",
    "run_ventus_multi_context_short.py"
  ],
  "commands": {
    "h100": ["{modal}", "run", "{suite}/modal_multi_context.py", "--gpu", "h100", "--project", "mixed", "--direction", "mixed", "--output", "{output}"],
    "b200": ["{modal}", "run", "{suite}/modal_multi_context.py", "--gpu", "b200", "--project", "mixed", "--direction", "mixed", "--output", "{output}"],
    "ventus": ["python3", "{suite}/run_ventus_multi_context_short.py", "--project", "mixed", "--direction", "mixed", "--output", "{output}"]
  }
}
